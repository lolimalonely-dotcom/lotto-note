# คีย์เลข (lotto-note)

เว็บช่วยคีย์รายการเลข แยกตามรอบ เลือกทุกอย่างด้วยการจิ้ม ไม่ต้องจำคำสั่งอะไร
พร้อมสรุปยอดและตรวจรางวัลอัตโนมัติ

Next.js 16 · React 19 · Tailwind v4 · Supabase

**ออกแบบให้อ่านง่ายจิ้มง่ายเป็นหลัก** — ตัวอักษรฐาน 17px ปุ่มสูงอย่างน้อย 48px
ทุกอย่างเลือกจากปุ่มหรือดรอปดาวน์ ไม่มีคีย์ลัดที่ต้องจำ

---

## กฎประเภท

| จำนวนหลักของรหัส | ประเภทที่เป็นไปได้                              |
| ---------------- | ---------------------------------------------- |
| 2 หลัก           | `บ.` · `ล.` · `วิ่ง บ.` · `วิ่ง ล.`              |
| 3 หลัก           | `ตรง` · `ต.` (โต๊ด) · `ล.` (3 ตัวล่าง)           |

พอใส่รหัส ปุ่มประเภทจะขึ้นมาเฉพาะอันที่เป็นไปได้เท่านั้น เลือกผิดไม่ได้เลย
กฎนี้บังคับทั้งฝั่งหน้าเว็บและฝั่งฐานข้อมูล (CHECK constraint)

### วิ่ง กับ กลับเลข ต่างกันยังไง

ใช้เลข `12` จำนวน `20` เหมือนกัน

|              | เก็บเงินลูกค้า        | คลุมเลข  | ถูกแล้วจ่าย                      |
| ------------ | -------------------- | -------- | -------------------------------- |
| **กลับเลข**  | 40 (แตกเป็น 2 บรรทัด) | 12, 21   | บรรทัดที่ถูก × 70                 |
| **วิ่ง บ.**   | 20 (ราคาเหมา)         | 12, 21   | 20 × 3 = 60                      |
| **วิ่ง ล.**   | 20 (ราคาเหมา)         | 12, 21   | 20 × 4 = 80                      |

วิ่งใช้ได้เฉพาะเลข 2 หลัก และบันทึกเป็นบรรทัดเดียว ไม่ต้องติ๊กกลับเลข

## รอบ

ทุกอย่างแยกตาม **รอบ** — เลือกรอบจากดรอปดาวน์บนสุดของหน้า ทำทีละรอบ

- กด **+ รอบใหม่** เพื่อตั้งชื่อรอบเอง พิมพ์อะไรก็ได้ (เช่น `16 ก.ย. 68`)
- รอบที่สร้างแล้วจะอยู่ในดรอปดาวน์ตลอด เลือกเอาไม่ต้องพิมพ์ซ้ำ จึงไม่มีรอบหลอนจากการพิมพ์ผิด
- สรุปยอดและตรวจรางวัลแยกตามรอบที่เลือกอยู่

## วิธีคีย์

1. เลือก (หรือเพิ่ม) **ชื่อรายการ**
2. ใส่ **รหัส** 2 หรือ 3 ตัวเลข — ติ๊ก **กลับเลขให้ด้วย** ถ้าต้องการ ระบบจะโชว์เลยว่าจะได้เลขอะไรบ้าง
3. **จิ้มเลือกประเภท** เลือกพร้อมกันหลายอย่างได้
4. ใส่ **จำนวน** — ประเภทละช่อง ใส่คนละจำนวนได้
5. กด **บันทึก** (ปุ่มบอกชัดว่าจะบันทึกกี่บรรทัด รวมเท่าไหร่)

ตัวช่วยความเร็ว

- ประเภทที่ใช้ล่าสุดถูกเลือกไว้ให้อัตโนมัติ และจำนวนค้างไว้ให้ คีย์เลขถัดไปจึงเหลือแค่ พิมพ์รหัส → กด Enter
- มีปุ่ม **ยกเลิกที่เพิ่งบันทึก** เผื่อกดพลาด และลบทีละบรรทัดมีถามยืนยันก่อนเสมอ

**กลับเลข** สร้างเลขสลับหลักทุกแบบที่ไม่ซ้ำกัน
`123` → 123, 132, 213, 231, 312, 321 (6 ตัว) · `112` → 112, 121, 211 (3 ตัว)

## สรุปยอด

- **ยอดรวมทั้งรอบ / จำนวนคน / บรรทัด / เลขไม่ซ้ำ** อยู่บนสุด
- **10 เลขที่เงินลงเยอะที่สุด** — แต่ละเลขแจกแจงว่าซื้อประเภทไหน รวมเท่าไหร่ ใครซื้อคนละเท่าไหร่
  เลขที่ซื้อน้อยไม่แสดง เพราะดูไปก็ไม่ช่วยตัดสินใจ
- **แยกตามชื่อคน** — ยอดรวมของแต่ละคน พร้อมทุกบรรทัดในบิล
- ส่งออก CSV (Excel อ่านภาษาไทยได้เลย) และสั่งพิมพ์ได้

## ตรวจรางวัล

พิมพ์ผลรางวัลทั้งหมด **ในช่องเดียว** คั่นด้วยเว้นวรรค จุด หรือขึ้นบรรทัดใหม่ ระบบแยกให้เองตามจำนวนหลัก
แล้วตรวจทุกรายการของรอบนั้นทีเดียว

```
123456 789 012 45
```

| เลขที่พิมพ์ | ระบบถือว่าเป็น                                          |
| --------- | ------------------------------------------------------ |
| 6 หลัก    | รางวัลที่ 1 → **3 ตัวตรง** = 3 หลักท้าย · **2 ตัวบน** = 2 หลักท้าย |
| 3 หลัก    | **3 ตัวล่าง** (ใส่ได้หลายตัว)                             |
| 2 หลัก    | **2 ตัวล่าง**                                           |

เลขความยาวอื่นระบบจะเตือนว่าอ่านไม่ออก ไม่เอาไปตรวจ

| ประเภท    | ถูกเมื่อ                                        |
| --------- | ---------------------------------------------- |
| บ.        | ตรงกับ 2 ตัวบน                                   |
| ล.        | ตรงกับ 2 ตัวล่าง                                 |
| วิ่ง บ.    | 2 ตัวบน เป็นเลขนี้หรือเลขกลับ                      |
| วิ่ง ล.    | 2 ตัวล่าง เป็นเลขนี้หรือเลขกลับ                     |
| ตรง       | ตรงเป๊ะกับ 3 ตัวตรง                               |
| ต. (โต๊ด)  | 3 ตัวตรง เป็นเลขนี้หรือเลขสลับหลักแบบใดก็ได้         |
| ล. (3 ตัว) | ตรงกับ 3 ตัวล่างตัวใดตัวหนึ่ง                        |

หน้าตรวจรางวัลแสดง **กำไร/ขาดทุนรวม**, **ต้องจ่ายใครบ้าง** (แยกรายคน) และ **บรรทัดที่ถูกทั้งหมด**
บรรทัดโต๊ดหรือวิ่งที่ถูกด้วยเลขกลับจะมีบอกว่า "(ออก 21)" ให้ตรวจทานได้

**อัตราจ่าย** (แทง 1 บาทได้กี่บาท) ซ่อนอยู่ใต้ปุ่ม *⚙ ดู / แก้อัตราจ่าย* แก้ได้ตลอด
ค่าตั้งต้น: บ./ล. 70 · วิ่ง บ. 3 · วิ่ง ล. 4 · ตรง 500 · โต๊ด 100 · 3 ตัวล่าง 110

ถ้ายังไม่ได้พิมพ์ผลส่วนที่บางประเภทต้องใช้ (เช่นยังไม่มีเลข 2 หลัก) บรรทัดเหล่านั้นจะยังไม่ถูกตรวจ
และมีข้อความบอกว่าค้างกี่บรรทัด

---

## รันในเครื่อง

```bash
npm install
npm run dev
```

เปิด http://localhost:3000

ถ้ายังไม่ตั้งค่า Supabase แอปจะวิ่ง **โหมดออฟไลน์** เก็บข้อมูลใน `localStorage` ของเบราว์เซอร์
(มีป้าย "ออฟไลน์" กำกับมุมซ้ายบน) ใช้งานได้ครบทุกฟีเจอร์ แค่ข้อมูลอยู่เฉพาะเครื่องนั้น

## ต่อ Supabase

1. สร้างโปรเจกต์ใหม่ที่ [supabase.com](https://supabase.com)
2. **SQL Editor** → วาง [`supabase/migrations/0001_init.sql`](supabase/migrations/0001_init.sql) ทั้งไฟล์ → Run
   แล้ววาง [`supabase/migrations/0002_check_results.sql`](supabase/migrations/0002_check_results.sql) → Run อีกครั้ง
   (0002 เพิ่มประเภทวิ่ง / 3 ตัวล่าง และตารางเก็บผลรางวัล/อัตราจ่าย — รันซ้ำหรือรันทับเวอร์ชันเก่าได้ไม่พัง)
3. **Authentication → Users → Add user** สร้างบัญชีของตัวเอง (ติ๊ก *Auto Confirm User*)
   — ตั้งใจไม่เปิดให้สมัครเองหน้าเว็บ เพราะ URL เป็นสาธารณะ
4. คัดลอก `.env.local.example` เป็น `.env.local` แล้วใส่ค่าจาก **Project Settings → API**

   ```
   NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
   ```

   ⚠️ ระวังใส่สลับช่อง — URL ต้องขึ้นต้น `https://` และคีย์ต้องขึ้นต้น `eyJ` หรือ `sb_publishable_`
   ถ้าใส่ผิดแอปจะไม่พัง แต่จะขึ้นแถบเหลืองบอกว่าผิดช่องไหน

**ความปลอดภัย** — ทุกแถวผูกกับ `user_id` และเปิด Row Level Security ไว้
แต่ละบัญชีเห็นและแก้ได้เฉพาะข้อมูลของตัวเอง บังคับที่ฝั่ง Postgres ไม่ใช่ฝั่งเบราว์เซอร์

## Deploy ขึ้น Vercel

1. push ขึ้น GitHub
2. Vercel → **Add New → Project** → เลือก repo
3. **Framework Preset ต้องเป็น `Next.js`** — ถ้าขึ้น `Other` ให้เปลี่ยนเอง
   ไม่งั้นจะ build ผ่านแต่เว็บขึ้น 404 เพราะมันไปเสิร์ฟโฟลเดอร์ `public/` แทน
4. ใส่ Environment Variables สองตัวข้างบน ชนิด **Config** (ไม่ใช่ Secret เพราะขึ้นต้นด้วย `NEXT_PUBLIC_`)
   ติ๊กให้ครบทั้ง Production / Preview / Development
5. Deploy — แก้ env ทีหลังต้อง **Redeploy** เสมอ

---

## โครงสร้างโค้ด

```
src/lib/rules.ts      ← กฎประเภทตามจำนวนหลัก และป้ายประเภท
src/lib/permute.ts    ← สร้างเลขกลับ ไม่ซ้ำ
src/lib/check.ts      ← แยกผลรางวัลตามจำนวนหลัก ตรวจทีละบรรทัด คิดเงิน
src/lib/summary.ts    ← รวมยอด 10 เลขที่เงินลงเยอะ และ CSV
src/lib/store.tsx     ← ชั้นข้อมูล สลับ localStorage / Supabase อัตโนมัติ + จัดการรอบ
src/components/       ← หน้าคีย์ แถบรอบ สรุปยอด ตรวจรางวัล
supabase/migrations/  ← สคีมา + RLS
```
